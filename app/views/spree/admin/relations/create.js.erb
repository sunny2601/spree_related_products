$('#products-table-wrapper').html('<%= escape_javascript(render "spree/admin/products/related_products_table", product: @product) %>');
$('#product_id').val('');
$('.actions a').tooltip();
$('.js-sort-handle').hide(); // sorting will not work after adding relations with ajax.
$('#addRelatedProduct').modal('hide'); // close the bootstrap modal
